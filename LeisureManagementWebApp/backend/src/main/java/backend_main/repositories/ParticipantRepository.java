package backend_main.repositories;

import backend_main.entities.Participant;
import backend_main.entities.embedded_ids.PersonId;

public interface ParticipantRepository  extends AbstractRepository<Participant, PersonId> {
}
